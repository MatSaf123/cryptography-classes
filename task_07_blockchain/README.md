(Docs are available at the home page of the project!)

To run:
```cmd
pip install -r requirements.txt
```

then simply:
```cmd
python controller.py
```

and then enter [localhost:8000](localhost:8000).
